/* 
  This file was generated by KaRaMeL <https://github.com/FStarLang/karamel>
  KaRaMeL invocation: /home/iamfo470/phd/everparse/opt/karamel/krml -fstar /home/iamfo470/phd/everparse/opt/FStar/out/bin/fstar.exe -fsopt --smt -fsopt /home/iamfo470/phd/everparse/opt/z3/z3-4.13.3 -tmpdir ../build -skip-compilation -skip-linking -minimal -no-prefix LowPLC.TP.Impl -bundle LowPLC.TP.Impl=LowPLC.TP,LowPLC.TP.Impl -drop FStar.*,LowStar.*,Prims -warn-error -2 LowPLC.TP.Impl.fst
  F* version: <unknown>
  KaRaMeL version: fb36fecb
 */

#include "LowPLC_TP_Impl.h"
#include "lowplc_krml_shim.h"

static K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs
tp_step(LowPLC_TP_tp_state s, bool in_val, uint32_t pt_ticks, uint32_t current_tick)
{
  bool rising_edge = in_val && !s.prev_in;
  switch (s.phase)
  {
    case LowPLC_TP_TP_IDLE:
      {
        if (rising_edge && pt_ticks > 0U)
          return
            (
              (K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs){
                .fst = {
                  .phase = LowPLC_TP_TP_TIMING,
                  .prev_in = in_val,
                  .start_tick = current_tick
                },
                .snd = { .q = true, .et_ticks = 0U }
              }
            );
        else
          return
            (
              (K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs){
                .fst = { .phase = s.phase, .prev_in = in_val, .start_tick = s.start_tick },
                .snd = { .q = false, .et_ticks = 0U }
              }
            );
        break;
      }
    case LowPLC_TP_TP_TIMING:
      {
        uint32_t elapsed = current_tick - s.start_tick;
        if (elapsed >= pt_ticks)
          if (!in_val)
            return
              (
                (K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs){
                  .fst = { .phase = LowPLC_TP_TP_IDLE, .prev_in = in_val, .start_tick = 0U },
                  .snd = { .q = false, .et_ticks = pt_ticks }
                }
              );
          else
            return
              (
                (K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs){
                  .fst = { .phase = LowPLC_TP_TP_DONE, .prev_in = in_val, .start_tick = 0U },
                  .snd = { .q = false, .et_ticks = pt_ticks }
                }
              );
        else
          return
            (
              (K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs){
                .fst = { .phase = s.phase, .prev_in = in_val, .start_tick = s.start_tick },
                .snd = { .q = true, .et_ticks = elapsed }
              }
            );
        break;
      }
    case LowPLC_TP_TP_DONE:
      {
        if (!in_val)
          return
            (
              (K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs){
                .fst = { .phase = LowPLC_TP_TP_IDLE, .prev_in = in_val, .start_tick = 0U },
                .snd = { .q = false, .et_ticks = 0U }
              }
            );
        else
          return
            (
              (K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs){
                .fst = { .phase = s.phase, .prev_in = in_val, .start_tick = s.start_tick },
                .snd = { .q = false, .et_ticks = 0U }
              }
            );
        break;
      }
    default:
      {
        KRML_HOST_EPRINTF("KaRaMeL incomplete match at %s:%d\n", __FILE__, __LINE__);
        KRML_HOST_EXIT(253U);
      }
  }
}

uint8_t tp_phase_to_u8(LowPLC_TP_tp_phase p)
{
  switch (p)
  {
    case LowPLC_TP_TP_IDLE:
      {
        return 0U;
      }
    case LowPLC_TP_TP_TIMING:
      {
        return 1U;
      }
    case LowPLC_TP_TP_DONE:
      {
        return 2U;
      }
    default:
      {
        KRML_HOST_EPRINTF("KaRaMeL incomplete match at %s:%d\n", __FILE__, __LINE__);
        KRML_HOST_EXIT(253U);
      }
  }
}

LowPLC_TP_tp_phase u8_to_tp_phase(uint8_t v)
{
  if (v == 0U)
    return LowPLC_TP_TP_IDLE;
  else if (v == 1U)
    return LowPLC_TP_TP_TIMING;
  else
    return LowPLC_TP_TP_DONE;
}

void
tp_step_c(
  uint8_t *phase_buf,
  uint8_t *prev_in_buf,
  uint32_t *start_buf,
  bool in_val,
  uint32_t pt_ticks,
  uint32_t cur_tick,
  uint8_t *q_out,
  uint32_t *et_out
)
{
  uint8_t phase_v = phase_buf[0U];
  bool prev_v = prev_in_buf[0U] != 0U;
  uint32_t start_v = start_buf[0U];
  LowPLC_TP_tp_state
  old_s = { .phase = u8_to_tp_phase(phase_v), .prev_in = prev_v, .start_tick = start_v };
  K___LowPLC_TP_tp_state_LowPLC_TP_tp_outputs
  result = tp_step(old_s, in_val, pt_ticks, cur_tick);
  LowPLC_TP_tp_state
  new_s = FStar_Pervasives_Native_fst__LowPLC_TP_tp_state_LowPLC_TP_tp_outputs(result);
  LowPLC_TP_tp_outputs
  out = FStar_Pervasives_Native_snd__LowPLC_TP_tp_state_LowPLC_TP_tp_outputs(result);
  phase_buf[0U] = tp_phase_to_u8(new_s.phase);
  uint8_t ite;
  if (new_s.prev_in)
    ite = 1U;
  else
    ite = 0U;
  prev_in_buf[0U] = ite;
  start_buf[0U] = new_s.start_tick;
  uint8_t ite0;
  if (out.q)
    ite0 = 1U;
  else
    ite0 = 0U;
  q_out[0U] = ite0;
  et_out[0U] = out.et_ticks;
}

