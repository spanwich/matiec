/* 
  This file was generated by KaRaMeL <https://github.com/FStarLang/karamel>
  KaRaMeL invocation: /home/iamfo470/phd/everparse/opt/karamel/krml -fstar /home/iamfo470/phd/everparse/opt/FStar/out/bin/fstar.exe -fsopt --smt -fsopt /home/iamfo470/phd/everparse/opt/z3/z3-4.13.3 -tmpdir ../build -skip-compilation -skip-linking -minimal -drop FStar.*,LowStar.*,Prims -warn-error -2 -no-prefix LowPLC.TOF.Impl -bundle LowPLC.TOF.Impl=LowPLC.TOF,LowPLC.TOF.Impl LowPLC.TOF.Impl.fst
  F* version: <unknown>
  KaRaMeL version: fb36fecb
 */

#include "LowPLC_TOF_Impl.h"
#include "lowplc_krml_shim.h"

static K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs
tof_step(LowPLC_TOF_tof_state s, bool in_val, uint32_t pt_ticks, uint32_t current_tick)
{
  switch (s.phase)
  {
    case LowPLC_TOF_TOF_IDLE:
      {
        if (!s.prev_in && in_val)
          return
            (
              (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                .fst = { .phase = LowPLC_TOF_TOF_DONE, .prev_in = in_val, .start_tick = 0U },
                .snd = { .q = true, .et_ticks = 0U }
              }
            );
        else if (in_val)
          return
            (
              (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                .fst = { .phase = LowPLC_TOF_TOF_DONE, .prev_in = in_val, .start_tick = 0U },
                .snd = { .q = true, .et_ticks = 0U }
              }
            );
        else
          return
            (
              (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                .fst = { .phase = s.phase, .prev_in = in_val, .start_tick = s.start_tick },
                .snd = { .q = false, .et_ticks = 0U }
              }
            );
        break;
      }
    case LowPLC_TOF_TOF_DONE:
      {
        if (!in_val)
          return
            (
              (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                .fst = {
                  .phase = LowPLC_TOF_TOF_TIMING,
                  .prev_in = in_val,
                  .start_tick = current_tick
                },
                .snd = { .q = true, .et_ticks = 0U }
              }
            );
        else
          return
            (
              (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                .fst = { .phase = s.phase, .prev_in = in_val, .start_tick = s.start_tick },
                .snd = { .q = true, .et_ticks = 0U }
              }
            );
        break;
      }
    case LowPLC_TOF_TOF_TIMING:
      {
        if (in_val)
          return
            (
              (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                .fst = { .phase = LowPLC_TOF_TOF_DONE, .prev_in = in_val, .start_tick = 0U },
                .snd = { .q = true, .et_ticks = 0U }
              }
            );
        else
        {
          uint32_t elapsed = current_tick - s.start_tick;
          if (elapsed >= pt_ticks)
            return
              (
                (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                  .fst = { .phase = LowPLC_TOF_TOF_IDLE, .prev_in = in_val, .start_tick = 0U },
                  .snd = { .q = false, .et_ticks = pt_ticks }
                }
              );
          else
            return
              (
                (K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs){
                  .fst = { .phase = s.phase, .prev_in = in_val, .start_tick = s.start_tick },
                  .snd = { .q = true, .et_ticks = elapsed }
                }
              );
        }
        break;
      }
    default:
      {
        KRML_HOST_EPRINTF("KaRaMeL incomplete match at %s:%d\n", __FILE__, __LINE__);
        KRML_HOST_EXIT(253U);
      }
  }
}

uint8_t tof_phase_to_u8(LowPLC_TOF_tof_phase p)
{
  switch (p)
  {
    case LowPLC_TOF_TOF_IDLE:
      {
        return 0U;
      }
    case LowPLC_TOF_TOF_TIMING:
      {
        return 1U;
      }
    case LowPLC_TOF_TOF_DONE:
      {
        return 2U;
      }
    default:
      {
        KRML_HOST_EPRINTF("KaRaMeL incomplete match at %s:%d\n", __FILE__, __LINE__);
        KRML_HOST_EXIT(253U);
      }
  }
}

LowPLC_TOF_tof_phase u8_to_tof_phase(uint8_t v)
{
  if (v == 0U)
    return LowPLC_TOF_TOF_IDLE;
  else if (v == 1U)
    return LowPLC_TOF_TOF_TIMING;
  else
    return LowPLC_TOF_TOF_DONE;
}

void
tof_step_c(
  uint8_t *phase_buf,
  uint8_t *prev_in_buf,
  uint32_t *start_buf,
  bool in_val,
  uint32_t pt_ticks,
  uint32_t cur_tick,
  uint8_t *q_out,
  uint32_t *et_out
)
{
  uint8_t phase_v = phase_buf[0U];
  bool prev_v = prev_in_buf[0U] != 0U;
  uint32_t start_v = start_buf[0U];
  LowPLC_TOF_tof_state
  old_s = { .phase = u8_to_tof_phase(phase_v), .prev_in = prev_v, .start_tick = start_v };
  K___LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs
  result = tof_step(old_s, in_val, pt_ticks, cur_tick);
  LowPLC_TOF_tof_state
  new_s = FStar_Pervasives_Native_fst__LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs(result);
  LowPLC_TOF_tof_outputs
  out = FStar_Pervasives_Native_snd__LowPLC_TOF_tof_state_LowPLC_TOF_tof_outputs(result);
  phase_buf[0U] = tof_phase_to_u8(new_s.phase);
  uint8_t ite;
  if (new_s.prev_in)
    ite = 1U;
  else
    ite = 0U;
  prev_in_buf[0U] = ite;
  start_buf[0U] = new_s.start_tick;
  uint8_t ite0;
  if (out.q)
    ite0 = 1U;
  else
    ite0 = 0U;
  q_out[0U] = ite0;
  et_out[0U] = out.et_ticks;
}

